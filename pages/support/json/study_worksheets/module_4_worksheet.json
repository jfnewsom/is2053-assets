{
  "_meta": {
    "purpose": "Single source of truth for the Module 4 Study Worksheet.",
    "philosophy": "Fill-in worksheet. Terms only — no definitions inline.",
    "lastEdited": "2026-05-18"
  },
  "module": 4,
  "moduleColor": "purple",
  "htmlTitle": "Module 4 Study Worksheet | IS2053",
  "topperTitle": "Module 4 Study Worksheet",
  "subBanner": "Strings, Dictionaries &amp; Sets &bull; Chapters 8 &amp; 9 &bull; Labs 4.1, 4.2, 4.3 &bull; Weeks 10&ndash;12",
  "intro": {
    "title": "How to Use This Worksheet",
    "body_html": "<p>This worksheet covers everything you need for the Module 4 Exam&mdash;plus professional Python practices that will serve you well beyond this course. Print it and fill it in by hand, or use the <strong>Copy as Rich Text</strong> / <strong>Copy as Plain Text</strong> buttons to paste it into Word and type your answers. If you work through this honestly, you&rsquo;ll be prepared. If you skip it, you&rsquo;re gambling. Your call.</p>"
  },
  "sections": [
    {
      "title": "Section 1: String Indexing and Slicing",
      "color": "blue",
      "content": [
        { "type": "section_intro", "text": "Strings are sequences. Access individual characters by index, or extract substrings with slicing." },

        { "type": "subheading", "text": "String Indexing" },
        { "type": "bullet_list", "items": [
          "Index (where does counting start?)",
          "Positive index",
          "Negative index",
          "Immutable (what does this mean for strings?)"
        ] },
        { "type": "prompt", "text": "If <code>city = &quot;Austin&quot;</code>: &nbsp; <code>city[0]</code> = ___ &nbsp; <code>city[5]</code> = ___ &nbsp; <code>city[-1]</code> = ___ &nbsp; <code>city[-2]</code> = ___" },

        { "type": "subheading", "text": "String Slicing" },
        { "type": "bullet_list", "items": [
          "Slice syntax (<code>s[start:end]</code>)",
          "Start index (included or excluded?)",
          "End index (included or excluded?)",
          "Default start",
          "Default end",
          "Step (third value)"
        ] },
        { "type": "fill_table",
          "headers": ["Expression (city = &quot;Houston&quot;)", "Result"],
          "rows": [
            ["<code>city[0:3]</code>", null],
            ["<code>city[3:]</code>", null],
            ["<code>city[:4]</code>", null],
            ["<code>city[::2]</code>", null],
            ["<code>city[::-1]</code>", null]
          ]
        },
        { "type": "key_question", "text": "Why does <code>string[0:3]</code> give 3 characters, not 4?" }
      ]
    },

    {
      "title": "Section 2: String Methods",
      "color": "cyan",
      "content": [
        { "type": "section_intro", "text": "String methods return NEW strings. The original is unchanged (strings are immutable)." },

        { "type": "subheading", "text": "Case and Whitespace Methods" },
        { "type": "bullet_list", "items": [
          "<code>upper()</code>",
          "<code>lower()</code>",
          "<code>capitalize()</code>",
          "<code>strip()</code>",
          "<code>lstrip()</code> / <code>rstrip()</code>"
        ] },

        { "type": "subheading", "text": "Search, Replace, and Testing" },
        { "type": "bullet_list", "items": [
          "<code>find(sub)</code> (what does it return if not found?)",
          "<code>replace(old, new)</code>",
          "<code>isdigit()</code>",
          "<code>isalpha()</code>",
          "<code>startswith(prefix)</code> / <code>endswith(suffix)</code>"
        ] },

        { "type": "subheading", "text": "Split and Join" },
        { "type": "bullet_list", "items": [
          "<code>split()</code>",
          "<code>split(sep)</code>",
          "<code>join(list)</code>"
        ] },
        { "type": "key_question", "text": "Why do string methods return new strings instead of modifying the original?" }
      ]
    },

    {
      "title": "Section 3: Dictionaries",
      "color": "cyan",
      "content": [
        { "type": "section_intro", "text": "Dictionaries store key-value pairs. Fast lookup by key, but keys must be unique and immutable. Use get() to avoid KeyError." },

        { "type": "subheading", "text": "Dictionary Concepts" },
        { "type": "bullet_list", "items": [
          "Dictionary",
          "Key (what makes a valid key?)",
          "Value",
          "Mutable (what does this mean for dicts?)",
          "Literal syntax (<code>{key: value}</code>)",
          "Empty dictionary (two ways to make one)"
        ] },

        { "type": "subheading", "text": "Accessing and Modifying" },
        { "type": "bullet_list", "items": [
          "Bracket notation",
          "<code>KeyError</code>",
          "<code>in</code> operator (what does it check?)",
          "Adding a key-value pair",
          "Deleting with <code>del</code>"
        ] },
        { "type": "key_question", "text": "Why must dictionary keys be immutable (like strings or numbers)?" },

        { "type": "subheading", "text": "Safe Access with get()" },
        { "type": "bullet_list", "items": [
          "<code>get(key)</code>",
          "<code>get(key, default)</code>",
          "Why use <code>get()</code> instead of <code>[]</code>?"
        ] },
        { "type": "prompt", "text": "<code>cities = {&quot;Austin&quot;: 165}; cities.get(&quot;Dallas&quot;)</code> returns ___" },
        { "type": "prompt", "text": "<code>cities = {&quot;Austin&quot;: 165}; cities.get(&quot;Dallas&quot;, 0)</code> returns ___" },

        { "type": "subheading", "text": "Iteration Methods" },
        { "type": "bullet_list", "items": [
          "<code>keys()</code>",
          "<code>values()</code>",
          "<code>items()</code>",
          "Shortcut: <code>for key in my_dict:</code>"
        ] }
      ]
    },

    {
      "title": "Section 4: Sets",
      "color": "cyan",
      "content": [
        { "type": "section_intro", "text": "Sets store unique values only. Duplicates are automatically removed. No indexing." },

        { "type": "subheading", "text": "Set Concepts" },
        { "type": "bullet_list", "items": [
          "Set",
          "Unique (what happens to duplicates?)",
          "Unordered (what does this mean for access?)",
          "Literal syntax",
          "From a list (<code>set([...])</code>)",
          "Empty set (and why <code>{}</code> doesn&rsquo;t work)"
        ] },

        { "type": "subheading", "text": "Set Operations" },
        { "type": "bullet_list", "items": [
          "<code>add(element)</code>, <code>remove(element)</code>, <code>discard(element)</code>",
          "Union <code>|</code>",
          "Intersection <code>&amp;</code>",
          "Difference <code>-</code>"
        ] },
        { "type": "key_question", "text": "Why does <code>{}</code> create an empty dictionary instead of an empty set?" }
      ]
    },

    {
      "title": "Section 5: Tracing &amp; Common Mistakes",
      "color": "orange",
      "content": [
        { "type": "section_intro", "text": "Trace string operations character by character. Track dictionary keys and values separately. And know what trips beginners up." },

        { "type": "subheading", "text": "String Method Tracing" },
        { "type": "fill_table",
          "headers": ["Expression", "Result"],
          "rows": [
            ["<code>&quot; hello &quot;.strip()</code>", null],
            ["<code>&quot;austin&quot;.upper()</code>", null],
            ["<code>&quot;a,b,c&quot;.split(&quot;,&quot;)</code>", null],
            ["<code>&quot;123&quot;.isdigit()</code>", null],
            ["<code>&quot;12.5&quot;.isdigit()</code>", null]
          ]
        },

        { "type": "subheading", "text": "Dictionary Tracing" },
        { "type": "fill_table",
          "headers": ["Code", "Dictionary Contents"],
          "rows": [
            ["<code>d = {'a': 1, 'b': 2}</code>", "{'a': 1, 'b': 2}"],
            ["<code>d['c'] = 3</code>", null],
            ["<code>d['a'] = 10</code>", null],
            ["<code>del d['b']</code>", null]
          ]
        },
        { "type": "prompt", "text": "<code>set([1, 2, 2, 3, 3, 3])</code> becomes ___" },
        { "type": "prompt", "text": "<code>{1, 2, 3} | {3, 4, 5}</code> is ___ &nbsp;&nbsp; <code>{1, 2, 3} &amp; {2, 3, 4}</code> is ___" },

        { "type": "subheading", "text": "Choosing the Right Data Structure (when to use each)" },
        { "type": "bullet_list", "items": [
          "List",
          "Dictionary",
          "Set"
        ] },

        { "type": "subheading", "text": "Common Module 4 Mistakes (what causes each?)" },
        { "type": "bullet_list", "items": [
          "Off-by-one in slicing",
          "Trying to modify strings",
          "<code>KeyError</code> with dictionaries",
          "Empty set confusion (<code>{}</code> vs <code>set()</code>)",
          "<code>isdigit()</code> and decimals"
        ] }
      ]
    },

    {
      "title": "Section 6: Concept Check",
      "color": "purple",
      "content": [
        { "type": "section_intro", "text": "If you can answer these, you understand the material. If not, go back and review." },
        { "type": "concept_questions", "start": 1, "items": [
          "Why can&rsquo;t you do <code>s[0] = 'X'</code> to change a character in a string?",
          "In <code>s[2:5]</code>, why is the character at index 5 not included?",
          "What&rsquo;s the difference between <code>find()</code> and <code>index()</code> for strings?",
          "Why use <code>dict.get('key')</code> instead of <code>dict['key']</code>?",
          "What happens if you add a duplicate element to a set?",
          "Why can&rsquo;t a list be used as a dictionary key?",
          "How do you create an empty set? Why not <code>{}</code>?",
          "What&rsquo;s the difference between <code>remove()</code> and <code>discard()</code> for sets?",
          "How would you count how many times each word appears in a text?",
          "When would you use a set instead of a list?",
          "What does <code>'12.5'.isdigit()</code> return and why?",
          "How do you reverse a string using slicing?"
        ] }
      ]
    }
  ],
  "closing": {
    "body_html": "<p><strong>That&rsquo;s everything.</strong> These data structures are the foundation of real-world programming. If you skimmed it, the exam will tell.</p>"
  }
}
